package com.gildedrose;

/**
 * Update strategy for Sulfuras items (legendary, never changes).
 */
public class SulfurasStrategy implements UpdateStrategy {
    @Override
    public void update(Item item) {
        // Legendary item, do nothing
    }
}
